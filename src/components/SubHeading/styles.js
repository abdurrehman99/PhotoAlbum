import {StyleSheet} from 'react-native';
import {subheadingFontSize, subheadingColor} from '../../utils';
export const styles = StyleSheet.create({
  subheading: {
    fontSize: subheadingFontSize,
    color: subheadingColor,
  },
});
